services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      FLASK_APP: app:create_app
      FLASK_ENV: development
      FLASK_DEBUG: "1"
    ports:
      - "5000:5000"
    volumes:
      - .:/app